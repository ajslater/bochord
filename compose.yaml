services:
  ci:
    build:
      context: .
      target: bochord-ci
    container_name: bochord-ci
    entrypoint:
      - sleep
      - infinity
    environment:
      - PYTHONUNBUFFERED=1
    image: bochord-ci:ci
    volumes:
      - ./test-results:/app/test-results
      - ./dist:/app/dist
